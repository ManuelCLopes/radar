name: CI

on:
  pull_request:
    branches: [ main ]

jobs:
  validate:
    runs-on: ubuntu-latest
    env:
      # Map database URL for all steps in the job - check both Secrets and Variables
      DEV_DATABASE_URL: ${{ secrets.DEV_DATABASE_URL || vars.DEV_DATABASE_URL }}
    strategy:
      matrix:
        node-version: [20.x]
    steps:
    - uses: actions/checkout@v6
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v6
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'
    - name: Install dependencies
      run: npm ci
    - name: Type check
      run: npm run check
    - name: Push DB Schema
      run: npm run db:push
    - name: Run unit tests
      run: npm test
    - name: Build
      run: npm run build

  e2e:
    needs: validate
    runs-on: ubuntu-latest
    env:
      DEV_DATABASE_URL: ${{ secrets.DEV_DATABASE_URL || vars.DEV_DATABASE_URL }}
    timeout-minutes: 60
    steps:
    - uses: actions/checkout@v6
    - name: Use Node.js 20.x
      uses: actions/setup-node@v6
      with:
        node-version: 20.x
        cache: 'npm'
    - name: Install dependencies
      run: npm ci
    - name: Install Playwright Browsers
      run: npx playwright install --with-deps
    - name: Run Playwright tests
      run: npx playwright test
    - uses: actions/upload-artifact@v6
      if: failure()
      with:
        name: playwright-report
        path: playwright-report/
        retention-days: 30
